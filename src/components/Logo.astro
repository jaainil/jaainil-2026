---
interface Props {
  className?: string;
}

const { className = "" } = Astro.props;
---

<a href="/" class={`flex items-center gap-3 hover:opacity-90 transition-opacity ${className}`}>
  <img src="/shravonix.png" alt="Shravonix" class="h-8 sm:h-10 w-auto" />
</a>